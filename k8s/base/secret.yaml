# EXAMPLE ONLY — do not commit real credentials. In a real cluster, generate this via
# your secret manager's K8s integration (External Secrets Operator, Sealed Secrets,
# Vault CSI driver, cloud provider's secret-sync controller, etc.) rather than applying
# a plaintext Secret manifest from source control.
apiVersion: v1
kind: Secret
metadata:
  name: payment-platform-secrets
  namespace: payment-platform
type: Opaque
stringData:
  mongo-uri: "mongodb://CHANGE_ME:CHANGE_ME@mongodb.payment-platform.svc.cluster.local:27017/payments"
