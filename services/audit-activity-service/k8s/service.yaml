apiVersion: v1
kind: Service
metadata:
  name: audit-activity-service
  labels:
    app: audit-activity-service
spec:
  selector:
    app: audit-activity-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
