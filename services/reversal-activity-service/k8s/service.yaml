apiVersion: v1
kind: Service
metadata:
  name: reversal-activity-service
  labels:
    app: reversal-activity-service
spec:
  selector:
    app: reversal-activity-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
