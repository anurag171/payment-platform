apiVersion: v1
kind: Service
metadata:
  name: mop-derivation-workflow-service
  labels:
    app: mop-derivation-workflow-service
spec:
  selector:
    app: mop-derivation-workflow-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
