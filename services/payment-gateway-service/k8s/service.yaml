apiVersion: v1
kind: Service
metadata:
  name: payment-gateway-service
  labels:
    app: payment-gateway-service
spec:
  selector:
    app: payment-gateway-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
