apiVersion: v1
kind: Service
metadata:
  name: warehouse-workflow-service
  labels:
    app: warehouse-workflow-service
spec:
  selector:
    app: warehouse-workflow-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
