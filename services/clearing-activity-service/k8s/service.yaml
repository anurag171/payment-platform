apiVersion: v1
kind: Service
metadata:
  name: clearing-activity-service
  labels:
    app: clearing-activity-service
spec:
  selector:
    app: clearing-activity-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
