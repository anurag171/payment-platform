apiVersion: v1
kind: Service
metadata:
  name: payment-processing-workflow-service
  labels:
    app: payment-processing-workflow-service
spec:
  selector:
    app: payment-processing-workflow-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
