apiVersion: v1
kind: Service
metadata:
  name: payment-simulator-service
  labels:
    app: payment-simulator-service
spec:
  selector:
    app: payment-simulator-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
