apiVersion: v1
kind: Service
metadata:
  name: repair-workflow-service
  labels:
    app: repair-workflow-service
spec:
  selector:
    app: repair-workflow-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
