apiVersion: v1
kind: Service
metadata:
  name: partner-activity-service
  labels:
    app: partner-activity-service
spec:
  selector:
    app: partner-activity-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
