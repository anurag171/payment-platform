apiVersion: v1
kind: Service
metadata:
  name: repair-activity-service
  labels:
    app: repair-activity-service
spec:
  selector:
    app: repair-activity-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
