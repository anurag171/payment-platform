apiVersion: v1
kind: Service
metadata:
  name: inbound-normalization-workflow-service
  labels:
    app: inbound-normalization-workflow-service
spec:
  selector:
    app: inbound-normalization-workflow-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
